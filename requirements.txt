# ---- LangChain (core + OpenAI integration) ----
langchain>=0.3             # framework: prompts, chains, memory
langchain-openai>=0.2      # ChatOpenAI model wrapper

# ---- App + config ----
streamlit>=1.37            # browser UI for the Q&A mini-project
python-dotenv>=1.0          # loads OPENAI_API_KEY from .env

# Optional, only if we demo Hugging Face via LangChain later:
#   langchain-huggingface
#
# After install works, lock exact versions:  pip freeze > requirements-lock.txt
