# ---- Secrets (NEVER commit these) ----
.env

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ---- RAG build artifacts (regenerated by `python -m src.ingest <pdf>`) ----
faiss_index/
*.db
*.sqlite3

# ---- OS noise ----
.DS_Store

# NOTE: data/sample.pdf IS committed on purpose so the app runs out of the box.
# Your own uploaded PDFs are read from a temp file and never written into the repo.
