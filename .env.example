# ============================================================
#  Copy this file to `.env` and paste in your real key.
#  `.env` is gitignored and must NEVER be committed.
# ============================================================

# OpenAI — LangChain's ChatOpenAI reads OPENAI_API_KEY automatically.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
